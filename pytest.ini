[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    ignore:Using padding='same':UserWarning
markers =
    slow: takes more than a few seconds (model forward/backward passes)
